{
    "name": "countIndicator",
    "category": "indicators",
    "config": {
        "name": {
            "type": "string",
            "minLength": 1
        },
        "startDate": {
            "$ref": "#/definitions/types/dateTime"
        },
        "rating": {
            "enum": [
                "neutral",
                "positive",
                "negative"
            ]
        }
    }
}